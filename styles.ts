import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  defaultText: {
    color: '#333', // Set your desired default text color here
    // You can also include other text styling properties if needed
  },
});
